name: form.block.fromfields.checkbox
icon: check

fields:
  columns:
    label: form.block.fromfields.checkbox.columns
    type: range
    min: 1
    max: 6
    default: 3

  options:
    label: form.block.fromfields.checkbox.options
    type: structure
    required: true
    columns:
      label:
        type: text
      selected:
        type: bool

    fields:
      label:
        label: form.block.fromfields.checkbox.options.label
        type: text
        required: true
        width: 1/3

      slug:
        label: form.block.fromfields.checkbox.options.slug
        type: slug
        sync: label
        required: true
        width: 1/3

      selected:
        label: form.block.fromfields.checkbox.options.selected
        type: toggle
        width: 1/3

